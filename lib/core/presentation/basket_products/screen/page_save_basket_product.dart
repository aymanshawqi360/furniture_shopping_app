import 'package:flutter/material.dart';

class PageSaveBasketProduct extends StatelessWidget {
  const PageSaveBasketProduct({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("PageSaveBasketProduct"),
        backgroundColor: Colors.amber,
      ),
    );
  }
}